# D430 / PX4 / OpenVINS 交接记录

**状态：ROOT CAUSE UNDER INVESTIGATION。没有通过动态验收，也没有飞行验证。**

本记录供下一位接手者直接续做，按实测证据区分已确认、未确认和已失效数据。PX4 视觉输出保持关闭；不要把 PX4 EKF 融合标志或静止表现当作 VIO 动态通过。

## 仓库与文件状态

- 本地仓库：`C:\Users\Admin\Desktop\无人机\Robomaster-vision-based-autonomous-flight-drone`，分支 `d430`。
- 最近确认的本地及 `origin/d430` HEAD：`072dd166e06ed774e4c13ea9eeb073116eb6967d`。前一阶段提交为 `e7ffdb03421f2e36683428a5570dbe572a5c43fc`。
- 检查时 HEAD 与 origin 一致，但工作区有未提交的数据分析和采集脚本：`scripts/analyze_sensor_timing.py` 的 gap 输出修改，以及 `analyze_imu_mapping.py`、`analyze_imu_noise.py`、`score_vio_static.py`、`start_vio_validation_capture.sh`、`trace_vio_imu.py`、`test_vio_static.py`。这些已在板上执行过部分验证，但还没有纳入 Git commit/push。新接手者先运行 `git status --short --branch`，保留并审阅这些文件，不要 reset/clean。
- 详细背景和原始时间 bag 指标：[根因与验证记录](VIO_ROOT_CAUSE_AND_VALIDATION.md)、[运行配置审计](VIO_RUNTIME_CONFIG_AUDIT.md)、[数据采集协议](VIO_DATASET_PROTOCOL.md)、[坐标约定审计](VIO_FRAME_CONVENTIONS.md)。较早的 `DEVELOPMENT_PROGRESS.md`、`CURRENT_STATE_AUDIT.md` 有过时历史段落，以本文件本次记录为准。

## 本次已完成的根因证据

### Camera–IMU 时间偏移

- 实际运行 YAML 是 `/home/orangepi/vio_ws/src/open_vins/config/d430/estimator_config.yaml`；相机链 YAML 未定义 `cam0.timeshift_cam_imu`，且 `calib_cam_timeoffset: false`。
- 对运行版本增加只读启动日志并在板端编译后，OpenVINS 实际打印 `LOADED_CAMERA_IMU_TIME_OFFSET_SEC=0.000000000`，同时告警固定偏移字段缺失。没有把估计器参数改成 -11 ms。
- `3c93dad` 的提交说明声称固定 -11 ms，但提交 diff 并没有写入该字段。历史 -11 ms 的来源仍未查明。
- OpenVINS `Propagator.cpp` 明确使用 `t_imu = t_cam + offset`；因此负11 ms 表示以图像时间减11 ms的位置选 IMU 样本。**最佳 offset、置信度和稳定性均未知**。目前没有带受控旋转标注的有效数据，不能确认 offset 是否导致动态发散。

### 相机与 CameraInfo

- D430 SDK 序列号 `938422073656`，USB descriptor/ASIC 序列号 `943623021659`，固件 `5.17.3.10`，USB SuperSpeed `5000M`，硬件 PID `0x0ad4`。
- 在 848×480 图像上，两路 CameraInfo 的 rectified P 内参与 OpenVINS YAML 完全一致：`fx=fy=422.2162476, cx=423.2308350, cy=240.2071381`；D=0、R=单位阵。右目 P 矩阵包含双目基线。数据见 `results/vio_validation/camera_info_comparison.json` 和 SDK 工厂标定文本。
- Stereo 的几何外参以及相机相对 PX4 IMU 的空间外参仍未经过正式物理标定。配置中的约8 cm IMU–camera 平移是安装假设，不能当作已验证值。

### 第一份 TIMING_ONLY bag

板端路径：`/home/orangepi/vio_data/20260913T151309_590262Z_TIMING_ONLY/bag`。它没有人工运动标签，不是 STATIC 数据集。实际约58秒：

| 项目 | 结果 |
|---|---:|
| infra1 | 1736帧，29.941 Hz |
| infra2 | 1719帧，29.648 Hz |
| 配对 | 1719对的 header timestamp 完全相同；17帧左图未配对 |
| IMU | 11352条，195.062 Hz；dt P95 5.557 ms、P99 9.874 ms、最大11.108 ms |
| IMU 重复/倒退时间戳 | 0 / 0 |
| 最近图像–IMU采样距离 | 左目 P99 3.638 ms；这不是时间标定 |

离线串口探测请求的是100 Hz，但 PX4 实际发来约200 Hz；没有 SYSTEM_TIME，也未记录 interval ACK。串口到达时间的60秒探测受初始队列积压影响，不能称为绝对延迟或稳定 jitter。

### 静止采集与 IMU 时钟 trace

操作者已确认组件放稳；静止采集于板端启动独立诊断栈，没有清除生产故障锁存。原始 bag：
`/home/orangepi/vio_data/20260913T152326_772057Z_STATIC/bag`，时钟 trace：
`/home/orangepi/vio_data/capture_20260913T152314Z/imu_time_mapping.csv`。

- bag 总时间跨度124.56秒；全体传感器消息有约2.26秒最大空档，另有约1.12秒和0.59秒空档；双目统计为3272对、336帧左图及7帧右图未配对。因此**这份 bag 不合格，不能用于参数 A/B，也不能作为不可变回归基准**。
- 桥接自身时钟 trace 在其中一个2.26秒 bag 空档内仍有445条 IMU，最大 mapped IMU 间隔9.89 ms。空档更像 recorder/DDS 丢消息，而不是 PX4 IMU 停发；需在下一次 bag 中用 trace 和 recorder 两边重新证实。
- 静止 OpenVINS `/odomimu` 可评分跨度118.251秒，没有达到120秒完整窗口，结果必须标为 **INCOMPLETE/FAIL gate**，不能算通过。已观测位置端点漂移8.95 mm、峰值9.99 mm、速度 RMS 0.00169 m/s、最大0.0103 m/s、位置方差峰值0.0273 m²。当前总是启用 ZUPT，这个结果可能被 ZUPT 掩盖，只能描述为该静止运行的观测值。
- 静止 IMU 有124.56秒描述性统计，均值 gyro `[-3.80e-5, 1.07e-5, -2.60e-5] rad/s`，std 约 `[0.001081,0.001088,0.001130] rad/s`；加速度均值 `[0.1005,-0.00644,9.80665] m/s²`，norm 均值9.8072 m/s²。少于5分钟，且没有 Allan variance；不能从样本 std 推断或修改连续时间噪声密度。
- bridge trace 比较 host、PX4 sample 和映射时间：181秒 trace 的 PX4 sensor timestamp 相对 host 的低分位趋势约 `-0.000927 s/s`，mapped 与 sensor 的净时长差约 `-0.179 s`。去趋势后的 residual P99 约7.56 ms。此结果暗示现有 lower-envelope 映射存在明显随时间变化的 clock offset，但 host 采样时刻仍包含调度/串口延迟；它不是独立测得的晶振漂移或绝对串口延迟。`SYSTEM_TIME` 映射仍未验证。

## 采集故障与飞行安全状态

- domain 42 使用默认 Fast DDS shared memory 时，camera/ROS participant 曾卡在 `SharedMemTransport::CreateInputChannelResource`。`fastdds shm clean` 清走430个 zombie segments 后没有解决。临时 UDP-only、限制至 `127.0.0.1` 的 systemd drop-in 曾让 ROS 节点恢复；该 drop-in 位于 `/run/systemd/system/vio.service.d/90-vio-audit-transport.conf`，重启后可能消失。此 workaround 不是飞行配置，也不是 VIO 修复。
- 23:17（2026-09-13）watchdog 检查到 `VIO_FATAL` 位置方差 `4.071 m²`，停止 VIO 并锁存 `/tmp/vio_fault_latched`。锁存与故障日志保存在板端 `/home/orangepi/vio_data/20260913_runtime_fault/`。之前记录显示 VIO 轨迹跑到数十米，但没有运动标签，所以不虚构轨迹误差。
- 最后一次**远程确认**：2026-09-13 23:22 CST 时 `vio.service=failed/stopped`、`vio-watchdog.service=active`、故障锁存存在，PX4 `send_vision_to_px4=false`。2026-09-24 SSH 到 `orangepi5`（`192.168.1.215`）连接超时，**当前运行状态未知**；上线后第一步只读检查 service、drop-in、锁存、USB、配置哈希和进程，不要直接删除锁存或启动生产服务。
- 从未在此轮修改 PX4 参数、自动解锁、起飞或声称飞行通过。

## 下次接手的明确步骤

1. 先 `git status --short --branch`、`git log -5 --oneline`，审阅未提交工具；确认 SSH 可达后仅查询板端状态。核对 `/tmp/vio_fault_latched`、`vio.service`、watchdog、相机 serial/firmware/USB speed、临时 systemd drop-in 是否仍在。
2. **不要清除生产故障锁存，也不要重启生产 PX4 bridge/VIO。** 用 `scripts/start_vio_validation_capture.sh` 启动独立采集栈。该脚本把诊断桥的 odometry 订阅重定向到 `/validation/no_flight_odometry`，bridge 的 `send_vision_to_px4` 明确为 false，并将相机、bridge、OpenVINS 和 rosbag 放入独立 `KillMode=control-group` unit。
3. 根据上次 SHM probe 结果先看其 bag 的 `metadata.yaml` 和 sensor gap；再选 domain 44 新域验证默认 SHM。如果 SHM 能稳定录制，固定使用此域采集；否则用已测试过的 UDP-only localhost profile，但把 bag 与 bridge trace 的时钟连续性一起检查。不要只依据 recorder 正常退出或 `metadata.yaml` 存在就验收。
4. 增大 bag 消息缓存（rosbag2 默认100 MiB；相机双目+IMU场景按至少约1秒数据量配，考虑双缓冲内存），监视板端磁盘、内存、CPU 和 recorder 警告。用同一段 trace 对齐验证 bag 空档是不是录制丢包。先做20秒诊断，只有所有核心 topic、header timestamp、完整帧数、无长 gap 都合格，才开始再次做人工静止采集。
5. 静止采集要给 OpenVINS 至少初始化预热时间；当前脚本虽录制125秒，里程计只有118.25秒。将 STATIC 录制提高到约140秒，实际评分仍只对连续完整120秒窗口进行。采集前告诉操作者保持静止，结束后明确告知可以移动。
6. 之后按相同可信 bag 进行运动信号 visual-angular-velocity vs gyro cross-correlation（搜索至少 -30…+30 ms）；再对相同 bag 做 ZUPT OFF 下 0、-11 ms、候选最优 offset 的单变量回放。保存 score curve、初始化状态、轨迹、ZUPT 决策、CPU/latency。未知时不能填 `-0.011`。
7. 继续标定 IMU 轴向、完整 camera–IMU 6DoF 外参、ZUPT 误触发和 5分钟 IMU噪声；审计本地 OpenVINS patch/upstream 与 EuRoC。完成 STATIC 后，仍需由操作者执行 ROTATION_ONLY、1 m TRANSLATION、实测尺寸 RECTANGLE。所有结果写明 **RELATIVE VALIDATION / NO EXTERNAL GROUND TRUTH**。
8. 仅当 stereo/timing/calibration/signs/ZUPT/dynamic bag/latency 全部通过后，才重新评估 PX4 融合。PX4 参数更改需先列出现值、目标值、原因和风险，再取得用户确认。飞行按分级计划执行；本记录不授权越级飞行。

## 当前结论

确认的配置回归候选是“提交说明称 -11 ms，但运行实际固定0 ms”。同时，桥接时钟映射显示显著时间趋势，ZUPT 一直开启，ROS recorder 曾丢帧。**目前无法在这些竞争因素中确定唯一动态漂移根因。最佳 camera–IMU offset、运动中是否错误触发 ZUPT、最终 feature 参数、patched/upstream 差异、独立 VIO 交叉验证与所有动态误差指标均待测。最终 baseline 不合格，飞行状态未验证。**
